# Handoff — session moved to `Max HQ/pqtools`

Written 2026-09-06 as the Max HQ-rooted session closed. A session opened in
this folder has the transcript and the memory index; this file is the
30-second version.

## State

Branch `feat/connectors-and-real-m-0.9.0`, tree **`100f346`**, clean, 26
commits since baseline `c7ffc5f`. Version 0.10.0. Nothing pushed, tagged or
published.

| | |
|---|---|
| Release gate | **PASSED 8/8** — 3981 tests sequential, 953 worked examples exact (`evidence/release-gate-2026-09-06-c7ffc5f..bd95332.log`) |
| Reviews | 8 `claude-opus-5` rounds; 7 and 8 returned **SHIP** (`evidence/opus5-review-round*.txt`) |
| Secret scan | clean under the hook's own ruleset and invocation |
| Audit | all 7 findings closed — `.planning/AUDIT-2026-09-05-CLOSEOUT.md` |

## The one thing still open

**The push needs G at a real terminal.** The pre-push hook's secret scan
passes; its final confirmation reads from a TTY, which an agent session does
not have. `GIT_PUSH_BYPASS` is *not* the remedy — it skips the scan.

    cd "/Users/gopalmacbook/Desktop/Max HQ/pqtools"
    git push -u origin feat/connectors-and-real-m-0.9.0

Answer `y`. Tag and PyPI publish remain G's separate call.

## Read in this order

1. `.planning/AUDIT-2026-09-05-CLOSEOUT.md` — every finding's disposition,
   all 8 review rounds, the requirement-to-evidence map, residual limits.
2. `SUPPORT-MATRIX.md` — the authoritative capability statement. Its numbers
   are test-enforced; do not edit them by hand.
3. `CLAUDE.md` — the invariants, including the three gates that exist because
   an invented name, arity and enum number each shipped once.

## Traps, learned the hard way in that session

- **This shell is zsh; the git hooks are bash.** Unquoted `${VAR}`
  word-splits in bash and does not in zsh. A reproduction of the pre-push
  hook returned "0 bytes" and read as a pass. Reproduce hook behaviour under
  `bash -c`, or the check is theatre.
- **Verify with the command that gates, not a cousin of it.** A
  `.gitleaksignore` was committed on the strength of a git-aware gitleaks run;
  the hook scans a `mktemp` diff with `--no-git`, where that fingerprint can
  never match. Replaced by `security/gitleaks.toml`.
- **A review finding is a claim.** One LOW was false — the path it described
  was already typed — and was fixed before being reproduced. Reproduce
  everything, LOWs included; rejecting on evidence is a valid disposition.
- **The estate autocommit bot commits inside this repo on a timer.** It once
  captured a positive-control mutation as HEAD. After any control that
  mutates a tracked file, check `git log -1` before committing.
- `DOCUMENTED_MATCHES` is asserted for **equality**, not as a floor. Raising
  it means running `scripts/sync_builtin_list.py` in the same commit so
  README.md and llms.txt follow.
- Run the suite **sequentially** on this 8 GB machine:
  `PQ_GATE_PYTEST_ARGS="" bash scripts/release_gate.sh`.

## Not verified, and not to be claimed

Live database, Fabric, Windows PQTest, native Excel and Power BI refresh were
never exercised. `semgrep` is not installed here. The three
`skipif(os.name == "nt")` guards were reasoned from repo precedent, not run
on Windows.

## Round 9 (later on 2026-09-06)

One commit on top of `de6e217`, subject `fix(core): refuse instead of
returning a truncated child payload; Value.FromText refuses temporal text`.
Gate 5 PASSED 8/8 on it (3999 tests). What it did, and the one trap, are in
`AUDIT-2026-09-05-CLOSEOUT.md`, section "Round 9". Read the subsection "The
BRIDGE_FAILURE, diagnosed - and the first diagnosis was wrong" before
touching `_run_process_bounded`'s timeout path: closing a reader pipe by
integer while a thread still holds the stream corrupts an unrelated later
call, and every instrumentation hides it.

Still G's call, unchanged: push, tag, PyPI. The estate autocommit bot swept
this repo twice more during round 9; `git log -1` before every commit.

### Round 9 reviewed

The `claude-opus-5` review of round 9 came back FIX-FIRST with six findings;
all six reproduced and all six are fixed in the commit after `d3bf107`
(dispositions in the closeout, "Round 9 reviewed"). Two were my own
reasoning inverted: stdin belongs in the timeout teardown (a grandchild
holding the read end otherwise leaves the writer stuck for good), and
`Value.FromText`'s temporal check asks the package's own parsers, not an
ISO regex. Positive controls for the three behavioural fixes are recorded
there, one discarded and redone.

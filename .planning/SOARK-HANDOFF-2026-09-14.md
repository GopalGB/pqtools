# Copy-paste handoff for soark llm

Paste the block below when you are ready to authorize execution. Merely creating this handoff did not authorize or begin implementation.

```text
Work in /Users/gopalmacbook/Desktop/Max HQ/pqtools.

You are taking over Claude Code's unfinished pqtools work. Execute the approved end-to-end plan at .planning/PLAN.md, one task at a time, and continue through all reversible in-scope work. Read .planning/RECOVERY-2026-09-14.md first, then the complete plan and applicable AGENTS.md/skills. If the executing-plans skill is unavailable in your environment, follow the written checklist sequentially with the same gates. Do not restart the historical round-15 handoff or rewrite already-delivered features.

Starting snapshot: branch feat/connectors-and-real-m-0.9.0, HEAD 6e1c90d0e1df00b0dfdc74247e0a8872df71dda8. Claude left a modified evidence/floor-venv-suite-2026-09-10.log. Preserve it. It records a completed historical run, not current validation. Check live state before acting; if it has changed, reconcile the difference instead of resetting it.

The plan covers a confirmed locale-dependent digest, failed/incomplete floor evidence acceptance, verification of a case-only rename issue in the review harness, actual product acceptance, docs and portfolio copy, dev/no-extras/minimum-version checks, packaging/security, exact Claude review and a publication handoff. Reproduce findings before fixing. Use existing code and tests; no speculative evaluator expansion or new runtime dependency.

Use .planning/CLOSEOUT-STATE-2026-09-14.md to record each checkpoint, the candidate SHA, test/review artifacts and the exact next command. Keep one heavy process at a time. Do not use pytest -n auto or split the suite and call it a full run. Do not disable unrelated automation or kill other processes.

Keep source and evidence stable during validation. Freeze/review code, generate evidence from that code, then verify the final candidate. Never hand-edit a produced test log, substitute a historic pass, or count skipped checks as passed. Full commands and success conditions are in PLAN.md.

Exact claude-opus-5 review and required security scans are mandatory. If the reviewer is unavailable, name REVIEW_BLOCKED and preserve the completed work; do not substitute another model or claim DONE. Treat a review suggestion as a claim to reproduce, not an instruction to apply blindly.

You may implement, validate, and create local checkpoints under this approved plan. Do not push, create a public PR, tag, publish to PyPI, deploy the website, change accounts, use credentials beyond an already-authorized verification, or send messages. Prepare the exact release artifacts, then ask G to approve the concrete public action. A v* tag can trigger PyPI publication.

Keep package local readiness, external CI, website deployment, PyPI publication, and native Microsoft-service validation separate. Explicitly retain the deferred dependency-graph/Fabric/PQTest/TMDL/folding/Mashup boundaries.

At the end, report what changed, actual test and review outcomes, any named blocker, and the one remaining G action. Do not claim universal Power Query compatibility or that public deployment occurred without live evidence.

Begin with Phase 1, first unchecked step.
```

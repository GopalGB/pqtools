# pqtools: Claude recovery and acceptance contract

Planning snapshot: 2026-09-14, Asia/Kolkata. This is a planning deliverable, not an execution or release receipt.

## User instruction

"check claude code recent session and all, whatever claude code left, you take it, MAKE plan, dont execyute make end to end plan thhen i will execute it using soark llm , plan should be perfect"

Executor name is preserved as **soark llm**. No model identifier, command, or capabilities are inferred from that name. The user selects it. This request authorizes investigation and writing the plan; it does not authorize executing it, pushing, tagging, publishing, deploying, or changing accounts.

## Recovered checkpoint

[HIGH] Repository: `/Users/gopalmacbook/Desktop/Max HQ/pqtools`. Branch: `feat/connectors-and-real-m-0.9.0`. HEAD: `6e1c90d0e1df00b0dfdc74247e0a8872df71dda8`. HEAD tree: `697ca147bc9619e1f6a4783d9226a64b0e64024d`. Package version in `pyproject.toml`: `0.10.0`. These were read from local Git and the manifest; remote HEAD and published version were not queried.

[HIGH] Before planning, the sole working-tree change was `evidence/floor-venv-suite-2026-09-10.log` (12 additions, 12 deletions). Preserve it. It is Claude's completed replacement run, not disposable output. The old committed copy records `bc34f1db...`; the replacement records `5d19e36e...`, 4146 passed, 41 skipped, and `FLOOR EXIT: 0`. Those are historical log contents, not tests run today.

[HIGH] Most recent substantive Claude transcript:
`/Users/gopalmacbook/.claude/projects/-Users-gopalmacbook-Desktop-Max-HQ-pqtools/e03a23c7-803e-451e-a99f-a968f3470b31.jsonl`.
It spans September 2-10 and covers the actual work through round 60. The newer-than-some-work `547f5ecb-5491-4b28-911f-f5a4d44d7804.jsonl` is a greeting-only session; it is not an execution checkpoint. Six recent project transcripts were inspected, with detailed recovery focused on the substantive session and repository artifacts.

Useful transcript locations (JSONL line numbers, not source-code lines):

| Lines | Evidence |
|---|---|
| 19804 | User asks for the end-to-end "pandas for Power Query" experience. |
| 22759, 23724 | User asks to finish the PRD and make pqtools' own diagnostics understandable. |
| 28974, 28997 | Earlier question about whether a smaller model can take over. |
| 30746-30786 | Claude corrects a release claim after the committed floor log fails freshness. |
| 30772-30773 | Replacement floor run launched. |
| 30819 | Stopped-task notification; this alone does not establish the process outcome. |
| 30825-30839 | Producer completion is read; replacement log and freshness pass in Claude's environment are recorded. |
| After 30839 | Session moves to unrelated WhatsApp/Ruchi work; do not import that into pqtools. |

The final exact-model review found is round 60, **reviewing `820c921..abb92ca`**, with `FIX-FIRST`. The fixes are in **`abb92ca..6e1c90d`**. A review of the former range is not a review of the latter. No final review closing the latter range was found in the inspected artifacts. `.planning/AUDIT-2026-09-05-CLOSEOUT.md:5778` explicitly leaves the full gate pending. `.planning/HANDOFF-2026-09-07-round15.md` is historical and must not be resumed as the current checkpoint.

Original later review receipt still available locally at planning time:
`/private/tmp/claude-501/-Users-gopalmacbook-Desktop-Max-HQ-On-Going-Project-Ruchi/e03a23c7-803e-451e-a99f-a968f3470b31/scratchpad/review-r60.md`.
`gate-r59.log` beside it says `GATE PASSED`, but describes a different dirty tree and 4185 tests. It cannot certify current HEAD.

## Confirmed new planning finding: locale-dependent digest

[HIGH] Read-only checks reproduced different digests for identical file bytes:

```text
LC_ALL=C bash scripts/floor_digest.sh
  d88455c71f45771b48b45ff727b6e52f2f5d5f06ab401e0bbcc3d53d0aedc799
LC_ALL=en_US.UTF-8 bash scripts/floor_digest.sh
  5d19e36eda5a2f847abb8af504c898adb84ddd92bcdcf40a1fe6c8d3732a4352
LC_ALL=en_IN.UTF-8 bash scripts/floor_digest.sh
  5d19e36eda5a2f847abb8af504c898adb84ddd92bcdcf40a1fe6c8d3732a4352
```

The current environment uses `LC_ALL=C.UTF-8`. `check_floor_freshness.sh` rejects the replacement log here as stale (exit 67); running the same checker under `LC_ALL=en_US.UTF-8` returns the selected path and exit 0. All **125** tracked files in the helper's scope were compared byte-for-byte to `git show HEAD:<path>`: **no differences**. A Python SHA-256 calculation over the same byte-sorted `shasum` records agrees with the C-locale digest. Therefore this failure is not evidence that source files changed: `sort -z` inherits collation, and the helper does not pin it. Patch the helper itself; an executor-shell locale override would only hide the defect for one caller.

The floor runner environment `/tmp/pq-floor-venv/bin/python` no longer exists. `.venv/bin/python` and both real sample workbooks exist. Presence does not verify dependency versions or workbook behavior.

## Acceptance contract and current implementation

Reuse `.planning/PRD-pandas-for-powerquery-2026-09-06.md` as the original product contract and `SUPPORT-MATRIX.md` as the current support statement. Reconcile these explicit historical differences in Phase 4 of PLAN.md; do not silently change the API to match old prose:

| Surface | Current implementation / evidence | Required closeout |
|---|---|---|
| Read, show, evaluate `.pq` and container queries | `export.PqFile`, `cli.py`, `tests/test_export.py`, `tests/test_container_workflow.py` | Real workbook loop on a copy plus current suite. |
| pandas / Arrow / parquet | `src/pqtools/export.py`, `tests/test_export.py` | Values AND dtypes; named refusals; real absent-extras environment. |
| CLI formats | Parser offers `--format json|csv`, `--to parquet --out PATH`; PRD proposed more `--to` aliases | Preserve the documented implementation and explicitly record the superseded PRD syntax. |
| Parameter binding | Current `--set-param` accepts M expressions under the query's I/O policy; PRD originally said scalar literals | Preserve current tested boundary; explicitly reconcile the contract and network-policy tests. |
| Batch | Read-only verbs accept paths/globs; destructive batches refuse | Verify successes, mixed failures, empty globs, exit codes, JSON, and no writes. |
| Easy diagnostics | `DIAGNOSTIC_HELP`, `FAILURE_HELP`, `pq explain`, clean `pq check` output | Exercise actual human output and stable machine output; do not redesign working behavior. |
| Earlier safety audit | Closeout dispositions and backup/OData/SQL regression tests | Revalidate; do not reimplement already-fixed findings from the September 5 audit. |
| Documentation / agent discovery | README, llms.txt, SUPPORT-MATRIX, checked fixtures | Remove unsupported claims; retain the names-versus-semantics distinction. |
| Dependency-range claims | Evidence measures selected 2.x/3.x pandas and Arrow versions, not every allowed version | Test actual declared minima or narrow the claim; an unbounded range has no tested "upper end". |
| Website links / plain-language page | Local portfolio commit `8e7feb1`; current links present | Copy refresh, build/visual check, separately approved deploy; PyPI README waits for release. |

Do not implement the deliberately deferred binding-aware query dependency graph, Fabric Arrow decoding/PQTest integration, `.pbip`/TMDL writes, folding, a full Mashup Engine, or Python DataFrame transformation methods. Keep their typed refusals and mark native-service behavior unverified.

## Related website checkpoint

[HIGH] Current local files in `/Users/gopalmacbook/Desktop/Max HQ/career-change/portfolio/site` contain the PyPI and GitHub links in `components/sections/work.tsx:163`. That card still says `299-function M evaluator`, `299 built-in M functions`, `680` tests, and `never a live SQL connector` (lines 154-161). This is stale relative to the current support matrix. The session reported a pending website deployment; public deployment status was not verified today. Treat this as a separate release item, not proof the links are live.

## Boundaries and completion criteria

The implementation is complete only when every PLAN.md phase has its own evidence: focused regressions, positive controls for changed behavior, one intact sequential full suite with extras, one intact no-extras run, successful current floor validation, JavaScript checks, package install smoke, required security scans, and exact `claude-opus-5` review of the final code. Review availability is untested today; missing access is a named execution blocker, not a planning failure or permission to substitute.

Local completion, external CI, public website deployment, and PyPI publication are separate states. No invented claim of "perfect", "all Power Query", or "secure" follows from green local tests.

## Investigation artifacts

Full selected session text is preserved locally at `/tmp/pqtools-claude-session-recovery-2026-09-14.txt`; relevant tool receipts are at `/tmp/pqtools-session-tail-tools-2026-09-14.txt`. These contain unrelated personal-session text, so do not commit or send the whole exports to reviewers. Use the narrow source references above. Temporary originals may disappear; recover from the named JSONL if needed.

Resource snapshot: 8 GiB RAM, roughly 9.3 GiB swap in use. It is a snapshot, not a threshold. Recheck before execution, keep one heavy job at a time, and never turn a chunked run into a full-suite pass.

# Exact claude-opus-5 wrapper review - range 4b8be7c..332b0d5 (bundle + lockfile shown as stub blobs)
# staged:  5 files changed, 328 insertions(+), 25 deletions(-)
# src/pqtools/_bridge.cjs: unchanged in 4b8be7c..332b0d5, not stubbed, not in the diff
# package-lock.json: unchanged in 4b8be7c..332b0d5, not stubbed, not in the diff
# invoked 2026-09-07T14:40:32Z via ~/.codex/skills/claude-review/bin/review.sh (ANTHROPIC_API_KEY unset inside the wrapper)
# NOTE (round 46): this capture's body carries the model marker `ox-alpha`.
# That is the model's own self-label under this machine's model-indicator rule,
# NOT evidence of provenance: review.sh invokes `claude -p --model claude-opus-5`
# and refuses to run on any substitution. Captures from round 46 onward record
# the requested model inline in their own header.

⚪ ox-alpha

I was asked to review a diff and reply in plain text only. Plan mode is active, so I won't create files — here is the review.

The diff deletes a broken `grep -q` gitlink net, replaces `| head -1` with parameter expansion, hoists `_GIT_ENV` into a `_git_env()` function, and adds two tests plus audit/evidence prose.

- **MEDIUM — scripts/gate_provenance.sh:94-108 — the deletion leaves the `if [ -n "$gate_add_err" ] … else … fi` intact but the comment block now sits between `fi` and `rm -rf "$gate_tmp"`, and the diff hunk header says 21 lines replaced by 41 while the visible context shows no `fi` before `rm -rf`.** If the removed `if [ -n "$gate_tree" ] && …; then … fi` block's closing `fi` was consumed with it, the script is fine; the diff as shown does not let a reader confirm the brace balance. Fix: run `bash -n scripts/gate_provenance.sh` and confirm the gate's own end-to-end test still exercises the dirty path (the release-gate log in this same commit certifies a *different* content id, `4b27df6…`, than the diff being reviewed).
- **MEDIUM — evidence/release-gate-2026-09-07-round20-fixes.log:2-5 — the evidence file certifies a tree that is not the staged tree.** Header says `commit: 4b8be7c (working tree DIRTY)` with `collect: 4139 tests`, and section 2 reports `4139 passed` — but this diff *adds two tests* on top of 4b8be7c, so a run that collected 4139 either predates the additions or the count coincidentally matches. This is exactly the failure recorded in `feedback_evidence_must_certify_the_tree_it_ran_on_2026-09-07`. Fix: re-run the gate on the staged tree and confirm `--collect-only -q | wc -l` equals the number in the header (expect 4141 if 4139 was the pre-change baseline).
- **MEDIUM — tests/test_end_to_end.py:1419+ — `test_a_declared_submodule_is_not_mistaken_for_a_stray_repository` cannot fail for the reason it claims, so it is not a control.** The net it guards against was *deleted* in this same commit; with no gitlink check present, this test passes for any implementation, including one that never looks at the tree. It is a regression guard, not a control — and the audit text explicitly counts it as one of "two behavioural" controls. Fix: either state in the docstring that it is a forward guard (as the large-tree test's docstring honestly does), or make it a real control by asserting against a reintroduced net in a subprocess-patched copy.
- **LOW — tests/test_end_to_end.py:988-1005 — `_GIT_ENV` is still assigned at module scope (`_GIT_ENV = _git_env()`) but no longer used by `_git()` or `_provenance()`.** Dead module-level state that will drift silently from `_git_env()`; a future reader may `env=_GIT_ENV` again and reintroduce the staleness the round-20 LOW was about. Fix: delete `_GIT_ENV` and update any remaining references, or keep it only if some other call site needs it.
- **LOW — tests/test_end_to_end.py:1276 — the skipif guard is `hasattr(os, "geteuid") and os.geteuid() == 0`, but the identical `chmod(0o000)` assumption exists in any sibling permission test.** Only one test was guarded. Fix: grep for other `chmod(0o000)` / `chmod(0o400)` uses in this file and apply the same guard, or hoist it to a shared `_skip_if_root` marker.
- **LOW — tests/test_end_to_end.py:1500+ — `test_the_identity_holds_for_a_tree_large_enough_to_sigpipe` writes 600 files and runs a full provenance pass on every suite run.** Given the recorded OOM/contention problem on this machine (`project_gate_needs_an_idle_machine_2026-09-07`), a 600-file fixture is a real cost for a guard against code that no longer exists. Fix: keep it, but confirm the 600 figure is the *minimum* that blocks a writer (a pipe buffer is 64 KB; ~233 entries × ~88 bytes ≈ 20 KB did not block per the audit's own numbers — so document the measured threshold rather than a round number).
- **LOW — .planning/AUDIT-2026-09-05-CLOSEOUT.md — the retraction is correct and well-stated, but the file now claims "Two behavioural [controls], each red with the defect and green after restoring" while one of those two (the submodule test) guards deleted code.** Fix: reword to "one control (`_git_env()`) and one forward guard".

Checked and not reported: the `${gate_add_err%%$'\n'*}` replacement is correct for zsh/bash and drops the SIGPIPE shape; the reasoning for deleting rather than repairing the gitlink net is sound (a declared submodule's SHA is exactly what HEAD records, and `git cat-file -e` genuinely rejects both declared and undeclared alike); the `protocol.file.allow=always` + `pytest.skip` fallback on `submodule add` is the right shape for git versions that refuse local submodule URLs.

FIX-FIRST

# wrapper exit: 2 (0=SHIP, 2=FIX-FIRST, 3=BLOCKED/no standalone verdict line)

# Exact claude-opus-5 wrapper review - range 29579d2..dccca7b (bundle + lockfile shown as stub blobs)
# staged:  7 files changed, 452 insertions(+), 48 deletions(-)
# src/pqtools/_bridge.cjs: unchanged in 29579d2..dccca7b, not stubbed, not in the diff
# package-lock.json: unchanged in 29579d2..dccca7b, not stubbed, not in the diff
# invoked 2026-09-08T03:38:42Z via ~/.codex/skills/claude-review/bin/review.sh (ANTHROPIC_API_KEY unset inside the wrapper)
# NOTE (round 46): this capture's body carries the model marker `ox-alpha`.
# That is the model's own self-label under this machine's model-indicator rule,
# NOT evidence of provenance: review.sh invokes `claude -p --model claude-opus-5`
# and refuses to run on any substitution. Captures from round 46 onward record
# the requested model inline in their own header.

⚪ ox-alpha

- **HIGH — src/pqtools/cli.py:930** — the shape test runs on `code = name.upper()`, so any ordinary snake_case M identifier is answered as a broken error code. Reproduced on this tree: `pq explain my_step` → *"MY_STEP is not a code this version of pqtools reports. The codes it does report are: M001, …"*, exit 0. Same for `source_data`, `raw_data_2`. The comment at cli.py:847 justifies the shape as safe because "no documented M name contains an underscore" — but this branch is only ever reached by names that are *not* documented and *not* in `BUILTINS`, i.e. exactly the "query name, a variable, a record field" the fall-through message names, and M identifiers do take underscores (the test itself asserts `let M_ = 1 in M_` parses). Round 25's defect with the sign flipped: a real name answered as a code. Fix: match on the raw input, not the upper-cased one — `_CODE_SHAPED.match(name)` — so only a literally all-caps input is treated as code-shaped.
- **MEDIUM — src/pqtools/cli.py:853** — the shape misses the lint-code family entirely, so llms.txt:166's promise is still false for the codes users hold most often. `M\d{3}` has no underscore: reproduced, `pq explain M007` → *"M007 is not a name pqtools recognizes as a documented Power Query M function. It may be a typo…"*, the exact answer this round set out to eliminate. `pq check` prints `M001`..`M006`, so a next-version lint code is the likeliest thing a user types. Fix: add the second shape — `^M[0-9]{3}$` — to the alternation, and add `M007` to `test_a_code_shaped_name_is_never_answered_as_a_function_name`.
- **MEDIUM — src/pqtools/core.py:154** — `severity: str = ""` is a defaulted field, so core.py:140's "One table cannot drift from itself" and tests/test_end_to_end.py:1580's "structurally impossible to omit" are both false. A new lint entry that omits `severity` constructs fine and prints `M007 (lint diagnostic)` with `"severity": ""` — the same wrong answer the removal of `DIAGNOSTIC_SEVERITY` was meant to make unreachable, still caught only by a test assertion. That is the shape this round graded MEDIUM one level out. Fix: drop the default and pass `severity=""` explicitly on the eleven failure entries, or give failures their own type.
- **LOW — llms.txt:163** — the new sentence "`severity` is the lint severity … or `""` for a failure code" is contradicted by the one code in both tables. Verified: `pq explain M_PARSE_ERROR --json` → `"kind": "lint diagnostic", "severity": "error"`, while llms.txt:133 lists `M_PARSE_ERROR` in the FAILURE table. An agent branching on the documented rule gets the opposite for it. Fix: say the failure table's `M_PARSE_ERROR` reports as a lint diagnostic because `check()` also emits it.
- **LOW — tests/test_end_to_end.py:1987** — the guard proves the shape does not swallow `DOCUMENTED`, but the branch it protects sits *before* `elif name in BUILTINS`, so a code-shaped builtin would be shadowed and nothing checks that set. Empty today (verified both lists are `[]`), which is why this is LOW and not the finding above. Fix: `for n in set(DOCUMENTED) | set(BUILTINS)`.
- **LOW — src/pqtools/cli.py:941** — the code-shaped branch prints the upper-cased `code` in `message` but `_print` emits `{"name": name, …}` with the raw input, so `pq explain m_future_error --json` returns `"name": "m_future_error"` alongside a message about `M_FUTURE_ERROR`. Fix falls out of the HIGH above; otherwise echo `name` in the message.

Checked and clean: no top-level `openpyxl`/`pandas`/`pyarrow` import exists in `src/`, so dropping `except Exception` from the module walk is safe as measured; `DIAGNOSTIC_SEVERITY` is referenced nowhere outside `cli.py` and prose, so its removal is not an API break; the `M_PARSE_ERROR` alias makes `DIAGNOSTIC_HELP[c] is FAILURE_HELP[c]` hold; `[A-Z0-9_]+` parses both llms.txt tables and the lint table's seven rows match `DIAGNOSTIC_HELP` including severities; the `invented` equality does redden on a code added to both `FAILURE_HELP` and llms.txt.

FIX-FIRST

# wrapper exit: 2 (0=SHIP, 2=FIX-FIRST, 3=BLOCKED/no standalone verdict line)

# Exact claude-opus-5 wrapper review - range dbd6f46..29579d2 (bundle + lockfile shown as stub blobs)
# staged:  8 files changed, 444 insertions(+), 10 deletions(-)
# src/pqtools/_bridge.cjs: unchanged in dbd6f46..29579d2, not stubbed, not in the diff
# package-lock.json: unchanged in dbd6f46..29579d2, not stubbed, not in the diff
# invoked 2026-09-08T03:15:51Z via ~/.codex/skills/claude-review/bin/review.sh (ANTHROPIC_API_KEY unset inside the wrapper)
# NOTE (round 46): this capture's body carries the model marker `ox-alpha`.
# That is the model's own self-label under this machine's model-indicator rule,
# NOT evidence of provenance: review.sh invokes `claude -p --model claude-opus-5`
# and refuses to run on any substitution. Captures from round 46 onward record
# the requested model inline in their own header.

⚪ ox-alpha

- **MEDIUM — src/pqtools/cli.py:866** — the llms.txt:165-166 promise "It never answers a real code as though it were an unrecognised function name" is enforced only by the test's derivation, not by the code. Reproduced on this tree: `pq explain M_FUTURE_ERROR` still prints *"is not a name pqtools recognizes as a documented Power Query M function. It may be a typo…"* and exits 0. Any code the derivation misses (a subclass setting `.code` on the instance rather than the class, a module the import loop swallows) reproduces round 25 verbatim — the same regime error one level out. Fix: after both table lookups fail, match `^(M_[A-Z_]+|NODE_ERROR|MQUERY_ERROR)$` and say "that is a pqtools failure code, not an M function name" rather than falling through to the typo branch.
- **MEDIUM — src/pqtools/core.py:256** — `FAILURE_HELP["M_PARSE_ERROR"]` is dead and already divergent. `diagnostic_help` is consulted first and `M_PARSE_ERROR` is in `DIAGNOSTIC_HELP` at core.py:150, so this entry can never be printed; the two texts already disagree (`"this is not valid Power Query"` vs `"the query is not valid Power Query"`, different `fix`). `test_every_code_the_cli_can_show_a_user_has_a_plain_english_entry` asserts on the *union*, so nothing can ever catch the drift. Fix: alias it — `"M_PARSE_ERROR": DIAGNOSTIC_HELP["M_PARSE_ERROR"]` — or assert `set(DIAGNOSTIC_HELP) & set(FAILURE_HELP)` entries are identical objects.
- **MEDIUM — tests/test_end_to_end.py:1762** — `except Exception: pass` around the `walk_packages` import loop silently shrinks the derived set: a module that stops importing takes its `MQueryError` subclasses' codes with it and the guard goes green while covering less. Verified latent today (0 import failures), but this is the guard whose whole claim is "derived rather than listed". Fix: collect the failures and assert they are only `ModuleNotFoundError` on a named optional-extra allowlist — a plain `ImportError` is a broken module, not an absent extra.
- **LOW — src/pqtools/cli.py:859** — `DIAGNOSTIC_SEVERITY.get(code, "")` replaces a loud `KeyError`. A lint code that gains a `DIAGNOSTIC_HELP` entry without a severity now prints `M007 (lint diagnostic)` and emits `"severity": ""` instead of failing. tests/test_end_to_end.py:1574 still asserts key-set equality, so the invariant holds at test time only. Fix: keep the subscript inside the branch where `diagnostic_help` hit; use `""` only on the failure path.
- **LOW — llms.txt:161** — the documented JSON shape lists `severity` but not that it is `""` for every failure code. Verified: `pq explain M_IO_ERROR --json` → `"severity": ""`. An agent branching on `severity` (the audience this file is written for) gets an empty string, not an absent key. Fix: state "empty when `kind` is `failure`", or omit the key there.
- **LOW — tests/test_end_to_end.py:1848** — `r"^\| \`([A-Z_]+)\`"` silently drops any documented code containing a digit; a future `M_ERROR2` row absent from `FAILURE_HELP` would leave both sets equal and the assertion green. Fix: `[A-Z0-9_]+`.
- **LOW — src/pqtools/cli.py:873** — `label` is the severity/kind string here and the column heading in the `for label, text in (...)` loop at cli.py:889. Two meanings, one name, sixteen lines apart. Fix: rename the first to `shown_kind`.
- **LOW — tests/test_end_to_end.py:1850** — the pair of guards proves "every emitted code has help" and "the two tables agree", but nothing proves the reverse: a code invented in both `FAILURE_HELP` and llms.txt that pqtools cannot emit passes every check. Fix: assert `set(FAILURE_HELP) - {"M_IO_ERROR"} ⊆` the runtime-walked class codes, with the batch literal as the one named exception.

Verified clean: the 12 `FAILURE_HELP` codes exactly match the runtime-derived set plus the `M_IO_ERROR` batch literal (reproduced independently); the llms.txt table index at line 131 does not collide with the lint table at 151; all 7 new/adjacent tests pass on this tree; the seven `getattr(error, "code", "M_IO_ERROR")` sites all carry the same literal, so probing one is sufficient for the default's value.

FIX-FIRST

# wrapper exit: 2 (0=SHIP, 2=FIX-FIRST, 3=BLOCKED/no standalone verdict line)

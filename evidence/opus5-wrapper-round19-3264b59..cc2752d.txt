# Exact claude-opus-5 wrapper review - range 3264b59..cc2752d (bundle + lockfile shown as stub blobs)
# staged:  7 files changed, 412 insertions(+), 40 deletions(-)
# src/pqtools/_bridge.cjs: unchanged in 3264b59..cc2752d, not stubbed, not in the diff
# package-lock.json: unchanged in 3264b59..cc2752d, not stubbed, not in the diff
# invoked 2026-09-07T13:59:17Z via ~/.codex/skills/claude-review/bin/review.sh (ANTHROPIC_API_KEY unset inside the wrapper)
# NOTE (round 50): this capture's body opens with a model self-label
# (`🔵 Opus 5`). That is the model's own label under this machine's
# model-indicator rule, NOT evidence of provenance: review.sh invokes
# `claude -p --model claude-opus-5` and refuses to run on any
# substitution. Captures from round 46 onward record the requested
# model inline in their own header.

🔵 Opus 5

- **MEDIUM — scripts/gate_provenance.sh:67 — `git add -A 2>/dev/null` drops paths while exiting 0, so `# content:` can still certify a tree that omits part of what ran** (the exact property the round-18 HIGH exists to guarantee). Two reproduced cases in a scratch repo running the staged script: (a) an unreadable directory — `add -A` prints `warning: could not open directory 'locked/'` to the discarded stderr, exits **0**, and `write-tree` returns an id byte-identical to a run where the file was absent; (b) a nested git repo (a stray clone or `git worktree` inside the tree — realistic here, gate runs live in `mq-gate-wt-*`) is written as `160000 commit <sha>` whose object is not in this store, so `git cat-file` on it fails while `git status --porcelain` counted it dirty — the DIRTY marker and the identity disagree, which is what the comment at :61 claims they cannot. Note an exit-status check would *not* catch either. Fix: `err=$(GIT_INDEX_FILE=... git add -A 2>&1)`; if `$err` is non-empty, or `git ls-tree -r "$gate_tree"` contains a `160000` entry, fall to the refusal branch.
- **MEDIUM — scripts/release_gate.sh:31-35 / gate_provenance.sh:78,102 — the FATAL text says "refusing to gate an unidentifiable tree", but the unidentifiable-tree case is precisely the one the guard cannot see.** `gate_provenance.sh` contains no `exit` and its last command is `printf '# date: …'`, so it always exits 0; `# content: unknown - COULD NOT IDENTIFY THE TREE THAT RAN` and `# collect: unknown - COLLECTION FAILED` both print their refusal and run on to `GATE PASSED`, exit 0. The `||` fires only when bash cannot execute the file (verified: 127 → FATAL, exit 2) — the single case the new test covers. Fix: `exit 3` from both `unknown` branches (keep the header printed), so the guard covers what its message claims.
- **LOW — tests/test_end_to_end.py:944-948 — `_GIT_ENV` pins config but not the git *environment*.** `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_TEMPLATE_DIR` and `GIT_CONFIG_PARAMETERS` (set by any `git -c …` ancestor, and by every git hook) are still inherited; under those the fixtures' `git init`/`add`/`commit` operate on the caller's repo and index rather than `tmp_path`, which is the same "result depends on who runs it" failure the change is written to close. Fix: build `_GIT_ENV` by also deleting those six keys.
- **LOW — scripts/gate_provenance.sh:87-91 — the fallback asserts "detached" without testing for it.** It fires whenever `git branch --show-current` prints nothing, which includes git < 2.22, where the option does not exist and the command errors to the suppressed stderr — so on an old git every run labels a normal branch `detached at <sha>`. Fix: gate the label on `git symbolic-ref -q HEAD` failing.
- **LOW — scripts/gate_provenance.sh:60 — "Same side effect as stash create" is not accurate.** `stash create` never wrote untracked content to the object store; `add -A` does, so every dirty gate run now stores blobs of every untracked non-ignored file (unreachable and gc-prunable, never pushed — but on disk, and outside what the pre-push secret scan looks at, which is commits). Worth stating in the comment given this repo's no-secret-value invariant.

Checked and not reported: the evidence log's `# content: c807236a` genuinely resolves — its blobs for `gate_provenance.sh`, `release_gate.sh` and `test_end_to_end.py` are byte-identical to the staged index, and the only path differing from the staged tree is the log writing itself, so this artifact does certify the tree it ran on. The round-17 `--verify` correction in the AUDIT is right: the inside-`||` form yields `[HEAD\nunknown]` and the outside form `[unknown]` (both reproduced), so `--verify` is defence in depth as stated. The rewritten content test does go red against `git stash create` (a stash commit's tree has no `untracked.txt`), the detached test goes red against `--abbrev-ref`, `$?` at release_gate.sh:32 expands to the child's real status, and the unborn-HEAD path still emits a complete identity despite `read-tree` failing.

FIX-FIRST

# wrapper exit: 2 (0=SHIP, 2=FIX-FIRST, 3=BLOCKED/no standalone verdict line)

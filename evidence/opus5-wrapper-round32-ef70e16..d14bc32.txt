# Exact claude-opus-5 wrapper review - range ef70e16..d14bc32 (bundle + lockfile shown as stub blobs)
# staged:  7 files changed, 550 insertions(+), 10 deletions(-)
# src/pqtools/_bridge.cjs: unchanged in ef70e16..d14bc32, not stubbed, not in the diff
# package-lock.json: unchanged in ef70e16..d14bc32, not stubbed, not in the diff
# invoked 2026-09-08T13:14:37Z via ~/.codex/skills/claude-review/bin/review.sh (ANTHROPIC_API_KEY unset inside the wrapper)
# NOTE (round 45): this capture's body carries the model marker `ox-alpha`.
# That is the model's own self-label under this machine's model-indicator rule,
# NOT evidence of provenance: review.sh invokes `claude -p --model claude-opus-5`
# and refuses to run on any substitution. Later captures record this inline.

⚪ ox-alpha

Verified by execution, not by reading: `pq check q.pq --format json` → **exit 0, no refusal**, while `--format csv` → refused; `pq rename q.pq --old a --new b --to parquet` → refused; `pq rename q.pq --from Old --to New` → argparse exit 2. `--allow-host`/`--bind`/`--set-param` are `action="append"` with no explicit default (None), so there is no shared-mutable-default hole in the new check. `_rename_blocker`'s `isascii()` guard is correct and the `min()` tie is unreachable. `.samples/` is absent in this worktree, so both new `pq add` tests skip here.

- **MEDIUM — src/pqtools/cli.py:1206** — the guard compares each option's VALUE against the parser default, so an option explicitly passed at its default value is still silently dropped. Measured: `pq check q.pq --format json` exits 0 and ignores `--format`; `--format csv` on the same verb is refused. `--format` (`cli.py:1301`, `default="json"`) is the one option this hits today, and it is the same option the docstring says drove the redesign. Fix: test PRESENCE, not value — give `--format` `default=None` and resolve to `"json"` after `_refuse_irrelevant_options`, or compare against the option strings actually present in `argv`.
- **MEDIUM — tests/test_end_to_end.py:2277** — the anti-drift guard reads the parser through a regex over source text (`parser\.add_argument\(\s*"--([a-z-]+)"`). An option declared with a short alias first (`add_argument("-o", "--out2")`), or one containing a digit or uppercase, never enters `defined`, so `defined == set(_OPTION_VERBS)` still passes and the new flag is universal-by-default — the exact drift the test exists to catch. The `verbs` set is likewise a hardcoded alternation, so a verb removed from the parser cannot be detected. Fix: build both from the parser object — `{a.dest for a in parser._actions if a.option_strings and a.dest != "help"}` and the `command` action's `choices`.
- **MEDIUM — llms.txt:32** — `pq rename report.pq --from Old --to New`: `--from` is not an option (argparse exits 2 with a usage dump) and `--to` is now a hard refusal on `rename`. This is the agent-facing file this diff edits, and an assistant quoting it verbatim now emits a command that cannot run. Fix: `pq rename report.pq --old Old --new New`.
- **LOW — src/pqtools/cli.py:1157** — `_OPTION_VERBS["json"]` lists `add`, but `_run_add`'s success path (`cli.py:445`, `cli.py:450`) prints raw text and never reads `args.json`; only the shared error path honours it. `pq add c.pbix --name X --source '...' --json` therefore drops the flag silently — the defect this round refuses everywhere else. Fix: emit JSON from the add preview/write path, or remove `"add"` from that frozenset.
- **LOW — src/pqtools/cli.py:427** — `pq add` now spawns Node twice on the preview path (0 before) and three times with `--write` (`write_sections` re-parses as part of its own verification, `containers.py:534`), each a 30 s-timeout subprocess, in a diff whose other half removes an ASCII loop for cost. The body parse only supplies the POSITION; it is not needed when the composed document parses. Fix: parse `new_source` first and parse `body` only in the failure branch, to choose which message.
- **LOW — src/pqtools/cli.py:441** — "the query parses alone but not inside this section" is asserted unconditionally, so a container whose EXISTING M does not parse is reported as the user's snippet breaking the composition. Fix: in that except branch (failure path only, no added cost when valid), parse `text` and say the container's existing section does not parse when that is what failed.
- **LOW — tests/test_end_to_end.py:2374** — `_one_section_container` reads `.samples/real-powerbi-fuzzy-matching.pbix`, which `.gitignore:16` excludes and CLAUDE.md says is never committed, so both round-31c guards skip in every clean clone — the preview/write disagreement is unguarded for anyone but you. `tests/test_write_validation.py:29` and `test_containers._blob()` already synthesize a one-section container in-repo. Fix: build the fixture from `_blob()` instead of `.samples/`.

FIX-FIRST

# wrapper exit: 2 (0=SHIP, 2=FIX-FIRST, 3=BLOCKED/no standalone verdict line)
